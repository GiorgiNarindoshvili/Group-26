import { useState } from 'react'


function App() {
  //1. the only difference between self-closing tag and container tag is that container tag must have child while self closing one not
  //2 children is element and it can be component, text or something else which would be inserted in container closing tags, we can use them using dot notation with props, every single component which is container tag can have children element, so component 
}

export default App
